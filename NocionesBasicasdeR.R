# Incio del codigo
# Aqui se debe especificar el para qué se creo y el que hace el código
# tmabién es bueno si se programa colaborativo poner fecha de creacion autor y fechas de modificacion y comentarios
# importante Procurar NO poner tildes o  ~n e~nes

rm(list = ls(all.names= TRUE)
gc()
